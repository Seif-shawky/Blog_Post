name: blog_projectbased
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  dartz: ^0.10.1

  flutter_bloc: ^9.0.0
  supabase: ^2.6.2
  fpdart: ^1.1.1
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
